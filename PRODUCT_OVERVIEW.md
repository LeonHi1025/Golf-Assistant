# 🏌️‍♂️ Golf Swing AI — LINE 智慧揮桿分析助理 產品功能介紹

## 一、 產品定位
結合 LINE Bot 與手機晶片端運算（Edge AI）的高爾夫揮桿力學分析助理。使用者只需上傳 3～10 秒正面揮桿影片，即可於手機本機秒級解析揮桿力學、比對國際職業標準，並自動回傳結構化診斷小卡與骨架疊加對照圖。

---

## 二、 核心功能特色

### 1. P1～P10 國際十階段揮桿相位自動辨識
運用時序平滑演算法與關節動力學，精準定位高爾夫揮桿全歷程十大關鍵幀：
* **上揚組**：P1 準備站姿、P2 起桿水平、P3 上桿半程
* **擊球組**：P4 上桿頂點、P5 下桿半程、P6 擊球前導、P7 擊球瞬間
* **送出組**：P8 送桿水平、P9 送桿半程、P10 收桿完成

### 2. 職業標準力學比對與偏差紅字提醒
* **角度力學診斷**：自動比對 P1 脊椎側傾角度，以及 P2～P10 各相位「手臂與軀幹夾角」。
* **相似度綜合評分**：計算全揮桿與標準動作之對齊百分比（75%～99%）。
* **醒目警示機制**：LINE 診斷小卡中，角度偏差過大（$\ge 10^\circ$）之關鍵相位，標題自動套用**紅色粗體**，直觀提示改善重點。

### 3. 【3 + 4 + 3】高清骨架對照圖
將 10 個相位自動合成為 3 組高品質對照長圖發送至 LINE：
* **雙層骨架疊加**：紫色國際標準骨架／球桿桿身 vs 綠色學員即時骨架／黃色桿身。
* **關節修正與防斷肢**：具備手腕防飄移、時間維度插值與手臂鏈條約束機制。

### 4. 分析完成自動閉合，流暢無縫體驗
* 手機完成影像特徵解析並同步數據後，**自動關閉網頁視窗**，強制將使用者切回 LINE 聊天室，即時查閱診斷處方小卡與對比圖片。

### 5. AR 即時幽靈骨架相機（Ghost Skeleton AR）
* **即時站姿對位**：相機畫面上方疊加 P1～P10 靜態向量幽靈骨架與站姿網格，供擊球前即時調整站位。
* **零運算負擔**：純向量圖層疊加，不消耗手機 AI 模型算力，畫面順暢不卡頓。
* **客製化輔助**：支援鏡像翻轉、骨架縮放、透明度與拖曳位移調整。
* **一鍵拍照存證**：支援快門聲、觸覺震動回饋與彈窗長按儲存／原生分享。

### 6. 極速隱私架構（Edge Computing）
* 影片解碼與 MediaPipe 骨架識別全數在**使用者手機本地（WebAssembly/GPU）**完成。
* 原始影片不上傳雲端，兼顧個人隱私、零等待上傳與伺服器零負載。

---

## 三、 系統架構與技術設計

本系統採用「**端點邊緣運算（Edge AI）＋ 輕量無狀態後端 ＋ LINE 原生生態**」的三層架構：

```
┌────────────────────────────────────────────────────────┐
│ 📱 使用者手機端 (LINE LIFF Webview)                    │
│  • 本地 GPU 運算：MediaPipe 姿態識別、P1～P10 相位切分 │
│  • 離屏長圖繪製：雙層骨架渲染、防斷肢約束、角度計算    │
│  • 數據同步上傳：僅上傳「分析數據」與「合成長圖」      │
│  • 視窗自動閉合：完成後關閉網頁，引導回 LINE 聊天室    │
└───────────────────────────┬────────────────────────────┘
                            │ POST /api/upload_report
                            ▼
┌────────────────────────────────────────────────────────┐
│ 🖥️ 後端轉發伺服器 (FastAPI / Render)                   │
│  • 數據暫存與轉發：無狀態架構，伺服器記憶體僅約 35MB   │
│  • 磁碟垃圾回收：常態保留最新 30 張，容量鎖定 < 5MB     │
│  • 格式化 Flex 卡片：偏差過大標題套用紅字粗體          │
└───────────────────────────┬────────────────────────────┘
                            │ LINE Messaging API (reply_token)
                            ▼
┌────────────────────────────────────────────────────────┐
│ 💬 LINE 官方帳號 (用戶端接收)                          │
│  • 零推播成本：採用雙向 reply_message，不消耗月配額    │
│  • 診斷小卡：相似度評分、偏差紅字提示、金色 AR 相機 Tip│
│  • 高清照片組：3 組【3+4+3】雙色骨架對照圖             │
└────────────────────────────────────────────────────────┘
```

### 架構三大核心優勢：
1. **邊緣 AI 零負載（Edge Computing）**：影像解碼與骨架預測 100% 於手機本機運算，無昂貴雲端 GPU 成本，後端維持極輕量。
2. **極致隱私保護**：原始揮桿影片不離開手機，僅上傳分析數值與去識別化的骨架長圖。
3. **零推播成本（Zero Quota Cost）**：透過 Webhook 雙向交握回覆，完全不佔用 LINE 官方帳號每月免費推播則數。

---

## 四、 使用流程

1. **加入官方好友**：進入 LINE 官方帳號點擊「分析」。
2. **上傳揮桿影片**：選取或拍攝 3～10 秒正面全身入鏡揮桿影片。
3. **手機本地解析**：系統即時演算 10 大關鍵相位、骨架連線與角度差。
4. **自動回傳診斷**：頁面自動關閉，LINE 聊天室即時收到：
   * 📊 **揮桿診斷小卡**（相似度評分、偏差紅字警示、逐張調整建議、金色 AR 相機捷徑）。
   * 🖼️ **3 張對比照片組**（P1～P3、P4～P7、P8～P10 雙色骨架對照）。
